define( [ 'd3', 'THREE' ], function( d3, THREE ){
  "use strict";

  function Painter( ){
    // this.Miembros;
  }

  Painter.prototype = {
    // Aqui va todo lo publico
    constructor: Painter,

    /* 
    FuncionPublica: function( arg1, arg2, ... ){
      ...
    },
    la ultima funcion publica NO lleva , al final 
    */
  };

  return Painter;
});
